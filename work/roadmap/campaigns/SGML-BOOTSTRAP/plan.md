# SGML-BOOTSTRAP Plan

1. Bootstrap repository metadata, ignore rules, and tracking.
2. Implement the Python service skeleton and runtime boundaries.
3. Add deterministic mock pipeline behavior and real adapter placeholders.
4. Update contract documentation with `preprocess.padding_px`.
5. Add tests, validate locally, and record evidence.
